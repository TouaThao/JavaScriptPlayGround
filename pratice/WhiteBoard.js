//In here we will add some whiteboard question and see if we could solve them 
// also what the logic use in the whiteboard

//We want to return amy not arnold what is wrong with this JS?
function greet (person) {
    if (person == { name: 'amy' }) {
      return 'hey amy'
    } else {
      return 'hey arnold'
    }
  }
  greet({ name: 'amy' })
console.log(greet)